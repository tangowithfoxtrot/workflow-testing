name: Hello World

on:
    push:
        branches:
            - main
    pull_request:
    workflow_dispatch:

jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello, World!"
    deploy:
        environment: testing-approvals
        runs-on: ubuntu-latest
        needs: hello-world
        steps:
            - name: Deploy Application
              run: echo "Deploying Application..."
